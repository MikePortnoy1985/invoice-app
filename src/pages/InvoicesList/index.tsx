import { FC } from 'react';
import TextField from '../../components/TextField';

const InvoiceList: FC = () => {
  return (
    <div>
      InvoiceList <TextField labelText="Street Address" />
    </div>
  );
};

export default InvoiceList;
